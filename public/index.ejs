<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Page de Connexion</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="p-4">
    <h1 class="text-2xl font-bold mb-4">Liste des utilisateurs</h1>
    <ul class="list-disc list-inside mb-4">
        <% users.forEach(user => { %>
            <li>
                <strong><%= user.username %></strong>
                (<%= user.email %>, <%= user.phone %>)
            </li>
        <% }) %>
    </ul>

    <h2 class="text-xl font-semibold mb-2">Ajouter un utilisateur</h2>
    <form action="/addUser" method="POST" class="space-y-2">
        <input type="text" name="username" placeholder="Nom d'utilisateur" required class="border p-2 w-full">
        <input type="email" name="email" placeholder="Email" required class="border p-2 w-full">
        <input type="password" name="password" placeholder="Mot de passe" required class="border p-2 w-full">
        <input type="number" name="phone" placeholder="Téléphone" required class="border p-2 w-full">
        <button type="submit" class="bg-blue-500 text-white p-2 w-full">Ajouter</button>
    </form>
</body>
</html>