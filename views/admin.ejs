<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Home - RubixCMS</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    :root {
      --dark-violet: #1a1a1d;
      --light-violet: #252525;
      --accent: #8a2be2;
      --hover-accent: #6a1cb5;
      --text-color: #e5e5e5;
      --border-color: #3d3d3d;
    }
  </style>
</head>
<body class="bg-[var(--dark-violet)] text-[var(--text-color)] flex flex-col min-h-screen">

  <!-- Header -->
  <header class="bg-[var(--light-violet)] w-full shadow-md py-4 px-6 flex justify-between items-center">
    <h1 class="text-2xl font-bold text-[var(--accent)]">RubixCMS bêta 1.0.0</h1>
    <nav>
      <a href="#" class="mx-4 hover:text-[var(--accent)]">Home</a>
      <a href="#" class="mx-4 hover:text-[var(--accent)]">Support</a>
      <a href="#" class="mx-4 hover:text-[var(--accent)]">Settings</a>
      <a href="#" class="mx-4 hover:text-[var(--accent)]">Staff</a>
    </nav>
    <a href="/logout" class="bg-[var(--accent)] text-white px-4 py-2 rounded-md hover:bg-[var(--hover-accent)] transition duration-300">
      Logout
    </a>
  </header>

  <!-- Contenu principal -->
  <main class="flex-grow flex flex-col items-center justify-start text-center p-6">
    <h2 class="text-4xl font-bold text-[var(--accent)]">Welcome, <%= user.username %>!</h2>
    <br>
    <p class="text-lg max-w-2xl">You are connected to the <span class="text-[var(--accent)]">Administrator</span> panel.</p>
    <br>
    <div class="bg-[var(--light-violet)] p-6 rounded-lg shadow-md">
      <h3 class="text-2xl font-bold text-[var(--accent)]">Total Users</h3>
      <p class="text-xl mt-2">There are <span class="text-[var(--accent)]"><%= totalUsers %></span> registered users.</p>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-[var(--light-violet)] w-full py-4 text-center shadow-md">
    <p class="text-sm">© 2025 RubixCMS bêta 1.0.0. All rights reserved. <a href="#" class="text-[var(--accent)] hover:underline">Terms of use</a></p>
  </footer>

</body>
</html>
